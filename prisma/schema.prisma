generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Tasks {
  id              String  @id @default(uuid())
  icon            String
  title           String
  description     String?
  status          String?
  backgroundColor String
}
